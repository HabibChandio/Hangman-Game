#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "functions.h"

int main()
{
	int choice;
	
	printf("-------------WELCOME TO HANGMAN -------------\n\n");
	
	printf("1. Start\n2. Leaderboard\n3. Quit");
	scanf("%d",&choice);
	do{
		if(choice == 1) {
			//
		}
		else if(choice == 2) 
			//
		else if(choice == 3)
			//
		else
			printf("Invalid input");
	  }while(choice != 3)
	return 0;
}
